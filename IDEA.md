単勝回収率（ROI）最大化のための次世代競馬AIモデル構築：スクレイピングデータに基づく特徴量エンジニアリングと数理的最適化の包括的調査

1. 序論：現代競馬予測におけるパラダイムシフト
1.1. 的中率から期待値への転換競馬予測における人工知能（AI）の適用は、過去数十年で劇的な進化を遂げた。初期の統計モデルや単純なルールベースのシステムは、主に「どの馬が勝つか」という的中率（Accuracy）の最大化に焦点を当てていた。しかし、パリミュチュエル方式（Parimutuel Betting）を採用している競馬市場において、的中率の追求は必ずしも経済的な成功、すなわち投資収益率（ROI: Return on Investment）の最大化を意味しない。多くの大衆が支持する人気馬は的中率が高い一方で、オッズは低くなり、過剰投票（Overbetting）によって期待値（Expected Value: EV）は往々にしてマイナスとなるからである 1。現代のデータサイエンスアプローチにおいて求められるのは、単に勝者を予測することではなく、市場の非効率性（Market Inefficiency）を発見し、大衆のバイアスによって歪められたオッズと、真の勝率（True Probability）との間の乖離（Edge）を捉えることである。これを達成するためには、公開情報を単なる数値として処理するのではなく、レースの文脈（Context）、展開（Pace）、トラックバイアス（Track Bias）、そして競走馬間の相互作用（Interaction）を深く理解し、モデル化する必要がある。本レポートでは、Webスクレイピングによって取得可能な公開データのみを使用し、特徴量エンジニアリング、モデル構造、損失関数、そして資金管理戦略の全方位から、単勝回収率を最大化するための技術的手法を包括的に論じる。

1.2. データの民主化と「情報の非対称性」の解消かつて、競馬予測における優位性は、厩舎関係者からのインサイダー情報や、専門家による主観的な相馬眼に依存していた。しかし、インターネットの普及とスクレイピング技術の高度化により、膨大なレースデータ、通過順履歴、ラップタイム、血統情報などが誰でも入手可能となった。これにより、情報の非対称性は縮小し、純粋なデータ解析能力とモデリング技術の優劣が勝敗を分ける時代に突入している。特に重要なのは、一見すると陳腐化した静的なデータ（例：過去の着順や通過順文字列）から、いかにして動的で高次な特徴量を抽出するかである。例えば、「3-3-4」という通過順文字列は、単なる数字の羅列ではなく、その馬がレースのどの局面でエネルギーを消費し、どの地点で加速あるいは失速したかを示す「エネルギープロファイル」の痕跡である 23。このような深層的な解釈を加えることで、公開データからアルファ（超過収益）を生み出すことが可能となる。

2. データ取得と前処理：隠された情報の採掘
2.1. スクレイピング対象の選定と構造化ROI最大化を目指すAIモデルにとって、データの質と量は生命線である。取得すべきデータは多岐にわたるが、特に以下の要素は必須である。基本属性: 馬名、年齢、性別、毛色、調教師、騎手、馬主、生産者。これらはエンティティ埋め込み（Entity Embeddings）の基礎となる 45。レース条件: 開催日、競馬場、コース（芝/ダート）、距離、天候、馬場状態。これらは環境変数としてモデルに入力される。過去走績（Past Performance）: 着順、走破タイム、通過順（Passing Order）、上がり3ハロンタイム、着差、負担重量。ここから時系列的な能力推移をモデリングする 6。オッズ履歴: 確定オッズだけでなく、時系列でのオッズ変動データが取得できれば、群衆心理（Wisdom of Crowd）やスマートマネーの動きを捉える特徴量となり得る 7。

2.2. 通過順（Passing Order）データの高度な処理多くの競馬サイトでは、レース中の馬の位置取りが「3-3-4」や「10-10-9-8」といったハイフン区切りの文字列で提供される。これは極めて重要な展開情報を含んでいるが、そのままでは機械学習モデルに入力できない。また、単純に数値化するだけでは、出走頭数の違いによるバイアスが生じる。

2.2.1. フィールドサイズによる正規化16頭立てのレースにおける「10番手」と、10頭立てのレースにおける「10番手」は、位置取りの意味合いが全く異なる。前者は中団やや後方であるが、後者は最後方（シンガリ）である。したがって、通過順位 $P$ は出走頭数 $N$ に基づいて正規化されるべきである。$$P_{norm} = \frac{P - 1}{N - 1}$$この式により、先頭は $0.0$、最後方は $1.0$ に正規化され、異なる出走頭数のレース間でも一貫した比較が可能となる。さらに、コーナーごとの通過順の変化（例：第3コーナー $0.5$ → 第4コーナー $0.2$）は、その馬がレース後半にポジションを押し上げた「マクリ（Mid-move）」の動きを示唆しており、この変化率（Derivative）を特徴量として抽出することで、馬の戦術的柔軟性や瞬発力を定量化できる 3。

2.2.2. 「道中不利」の推定通過順データとラップタイムを組み合わせることで、馬が道中で「無駄な動き」をしたかどうかを推定できる。例えば、ハイペース（前半のラップが速い）のレースにおいて、通過順が「2-2-2」のように先行し続けた馬が最後失速した場合、それは能力不足ではなく「展開不向き」であった可能性が高い。逆に、スローペースで後方待機（「15-15-14」）のまま敗れた場合も同様である。このように、通過順データをレースのペースコンテキストと照らし合わせることで、着順という表面的な結果の背後にある「負けの質」を評価することが可能となる 2。

2.3. カテゴリ変数の処理と高次元性の克服騎手、調教師、種牡馬（Sire）などのIDは、カーディナリティ（カテゴリの数）が非常に大きい。例えば、日本の競馬における種牡馬は数千頭に及ぶ。これを従来のOne-Hotエンコーディングで処理すると、入力次元が数万に達し、スパース（疎）なデータとなって学習効率が著しく低下する（次元の呪い）。

2.3.1. エンティティ埋め込み（Entity Embeddings）この問題を解決するために、自然言語処理で用いられる単語埋め込み（Word Embeddings）と同様の手法を適用する。各カテゴリ（ID）を低次元の密ベクトル（Dense Vector）に変換し、ニューラルネットワークの学習過程でその値を最適化する 89。埋め込み次元数 $d$ の決定には、経験則（Rule of Thumb）が存在する。FastAIなどのフレームワークで推奨される指針として、カテゴリ数 $C$ に対して以下の式が用いられることが多い 1010。$$d = \min(600, \text{round}(1.6 \times C^{0.56}))$$あるいは、より簡易的には、$$d = \min(50, (C+1)/2)$$$$d = \min(50, C^{0.25})$$この埋め込み層を通じて学習されたベクトルは、意味的な類似性を持つようになる。例えば、「ディープインパクト産駒」と「ハーツクライ産駒」は共に芝の中長距離を得意とするため、ベクトル空間上で近い位置に配置される。これにより、未知の馬や出走回数の少ない騎手に対しても、類似の属性を持つエンティティからの推論が可能となり、モデルの汎化性能が向上する 1112。

3. 特徴量エンジニアリング：物理モデルと統計的推論の融合生データの前処理を超えて、競馬のドメイン知識に基づいた高度な特徴量を生成することが、モデルの性能を決定づける。ここでは、ペース指数、トラックバイアス、そして展開予測のための指標について詳述する。

3.1. ペース指数（Pace Figures）の精密計算走破タイム（Final Time）は、そのレースのペースに大きく依存するため、絶対的な能力指標としては不十分である。スローペースのレースでのタイムは遅くなり、ハイペースでは速くなる傾向がある（ただし、超ハイペースで共倒れする場合を除く）。真の能力を測るためには、区間タイム（Fractional Times）を用いたペース分析が不可欠である。

3.1.1. Brisnet流のペース指数（E1, E2, LP）米国のBrisnet社が採用しているペース指数は、レースを複数のセグメントに分解し、それぞれの区間でのパフォーマンスを評価する優れた手法である 13。これを日本の競馬データに適用する場合、以下のように定義できる。E1 (First Call Pace): スタートから最初の分岐点（通常は2ハロンまたは4ハロン地点）までの速さ。これは馬の「テンの速さ（出足）」を示す。E2 (Second Call Pace): スタートから第2の分岐点（通常は4ハロンまたは6ハロン地点）までの速さ。これは「道中の追走力」を示す。LP (Late Pace): 第2分岐点からゴールまでの速さ。いわゆる「上がり」の脚であり、末脚の切れ味を示す。これらの数値を計算するためには、各地点での通過タイムが必要となる。スクレイピングデータにおいてラップタイムが完備されている場合、以下の式で速度（feet per second等）を算出し、標準的なスケール（例えば、基準タイムに対する偏差）に変換する 1415。$$\text{Velocity} = \frac{\text{Distance}}{\text{Time}}$$$$\text{Rating} = 100 - C \times (\text{Par Time} - \text{Actual Time})$$ここで $C$ は定数であり、1秒あたりのポイント数（例えば2ポイント/秒）を表す。このペース指数を用いることで、モデルは「前半無理をして飛ばしたために後半失速した馬」と「余力を残して先行し、そのまま押し切った強い馬」を区別することが可能になる。

3.1.2. ターンタイム（Turn Time）の重要性特にダート戦や小回りコースにおいて、勝負の分かれ目となるのが「ターン（コーナー）」での加速力である。直線だけのスピードではなく、遠心力に抗いながらコーナーで加速できる能力は、高い競走能力の証明である。ターンタイムは、コーナー入口と出口の通過タイムの差分として計算される「隠された区間（Hidden Fraction）」であり、多くの一般ファンが見落とすエッジの源泉となる 1414。$$\text{Turn Time} = \text{Time}_{\text{Stretch Call}} - \text{Time}_{\text{Pre-Turn Call}}$$このターンタイムが優秀な馬は、コーナーワークが巧みであり、4コーナー出口で有利なポジションを確保できる確率が高い。

3.2. トラックバイアス（Track Bias）の動的検知「内枠有利」「逃げ有利」といったトラックバイアスは、コース形状による恒久的なものと、当日の馬場状態（含水率、芝の生育状況、内側の荒れ具合）による一時的なものに分類される。特に重要なのは後者であり、これはリアルタイムに変動する 1617。

3.2.1. ローリング・バイアス指標（Rolling Bias Index）当日のレース結果をリアルタイムで監視し、バイアスを定量化する。例えば、その日の第1レースから第$k$レースまでの結果を用いて、以下の指標を算出する。Position Bias: 勝ち馬および連対馬の4コーナー通過順位（正規化済み）の平均値。この値が0.2以下であれば極端な「前残り」、0.8以上であれば「差し・追込有利」と判定する 18。Path Bias: 勝ち馬の通ったコース取り（内ラチからの距離）。これには詳細なトラッキングデータが必要となる場合があるが、枠番（Draw）で代用することも可能である。内枠（1-3枠）の勝率が異常に高い場合は「内枠有利」バイアスが存在する 19。これらの指標を時系列データとして扱い、Pandasの rolling().mean() 関数などを用いて平滑化し、次のレースの予測特徴量として投入する 20。

3.2.2. バイアス適合スコア（Bias Fit Score）算出されたトラックバイアスと、各馬の脚質（Run Style）の適合度を計算する。Run Style: 各馬の過去走の通過順データから、「逃げ」「先行」「差し」「追込」のいずれかに分類する 2122。Fit Score: 現在のバイアス（例：逃げ有利）に対して、その馬の脚質（例：逃げ）がどれだけ合致しているかを数値化する。バイアスと脚質が一致する場合、その馬の評価を大幅に上方修正する。これにより、能力値（スピード指数）だけでは見抜けない穴馬を発見できる 2317。

4. モデルアーキテクチャ：相互作用と集合の学習競馬予測を「単一の馬の勝率予測（回帰または二値分類）」として捉える従来のアプローチには限界がある。競馬は相対評価のゲームであり、ある馬が勝つかどうかは、他に出走する馬の能力や脚質との相互作用（Interaction）によって決まるからである。例えば、強力な逃げ馬が複数いる場合、ハイペースとなって共倒れし、差し馬が台頭する可能性が高まる。このような「レース力学」をモデル化するためには、セットベース（集合ベース）のニューラルネットワークが必要となる。

4.1. Permutation Invariant Neural Networks (Deep Sets / Set Transformer)レースに出走する馬のリストは「集合（Set）」であり、順序を持たない（馬番1, 2, 3...という順序は便宜的なものであり、本質的な意味を持たない）。また、出走頭数はレースごとに異なる（可変長）。このようなデータを扱うには、入力の順序置換に対して出力が不変（Permutation Invariant）または等価（Permutation Equivariant）であるアーキテクチャが必要である 242526。

4.1.1. Set Transformerによる相互作用のモデリングSet Transformerは、TransformerのSelf-Attention機構を集合データに応用したものである。各馬をトークンと見なし、レース全体をシーケンスとして入力することで、馬同士の相互作用をAttention Weightとして学習する 27。Encoder: 各馬の特徴ベクトル（$h_i$）を入力とし、Multi-Head Self-Attention (MHA) を用いて、他馬との関係性を考慮した新たな特徴表現（$h'_i$）を生成する。$$H' = \text{LayerNorm}(H + \text{MHA}(H, H, H))$$ここで、$H$は全出走馬の特徴行列である。この層により、モデルは「逃げ馬が他に何頭いるか」「圧倒的な1番人気馬との能力差はどれくらいか」といったコンテキストを各馬の特徴量に埋め込むことができる 2829。Interaction Logic: Self-Attention機構は、数学的には各馬の特徴量の加重平均を計算することと等価であり、これはレース展開のシミュレーションをニューラルネットワーク内部で行っていると解釈できる 30。例えば、先行馬同士のAttention Weightが高くなるように学習されれば、それは先行争いの激化をモデルが検知していることを意味する。

4.1.2. Pooling by Multihead Attention (PMA)エンコーダーによって更新された各馬の特徴量を集約（Pooling）し、レース全体を表すベクトル（Global Race Context）を生成する。Deep Setsでは単純なSumやMeanを用いるが、Set TransformerではPMAを用いることで、より適応的な集約が可能となる。このレースコンテキストベクトルは、そのレースが「ハイレベル戦」なのか「低レベル戦」なのか、あるいは「波乱含み」なのかといった全体像を表す 27。

4.2. 時系列データの処理：LSTM vs Transformer各馬の過去の戦績（History）は時系列データである。これを処理するモジュールとして、LSTM（Long Short-Term Memory）やGRU、あるいは時系列特化型Transformerが考えられる。

4.2.1. 時系列Transformerの優位性近年では、LSTMに代わりTransformerを時系列データに適用する事例が増えている 3132。Transformerは並列計算が可能であるため学習が高速であり、かつAttention機構により「直近のレース」だけでなく「1年前の同条件のレース」のような長期的な依存関係を捉えることができる。Positional Encoding: レース履歴の順番（時系列順）を保持するために、Positional Encodingを加算する。ただし、競馬の履歴は必ずしも等間隔（日付）ではないため、経過日数を考慮したTime-aware Positional Encodingが有効な場合がある 3334。

4.3. 全体アーキテクチャの構成推奨されるモデル構造は、以下のハイブリッド型である。Horse History Encoder: 各馬の過去走データを時系列TransformerまたはLSTMで処理し、馬ごとの「現在状態ベクトル（Current State Vector）」を生成する。Current Race Contextualizer: 全出走馬の現在状態ベクトルと、今回のレース条件（距離、馬場など）を結合し、Set Transformerに入力する。ここで馬同士の相互作用を計算する。Scoring Layer: Set Transformerの出力（相互作用考慮済みの特徴ベクトル）を全結合層（MLP）に入力し、各馬の「勝率スコア（Logits）」を出力する。

5. 損失関数と学習戦略：ランキングと確率の最適化モデルの構造が決まれば、次は「何を最適化するか」という損失関数（Loss Function）の設計が重要となる。単勝ROIを最大化するためには、回帰問題（着順の予測）や単純な分類問題（1着か否か）として解くよりも、「ランキング学習（Learning to Rank）」のアプローチが優れている。

5.1. Pointwise, Pairwise, Listwiseアプローチの比較ランキング学習には主に3つのアプローチがある 3536。アプローチ概要メリットデメリット競馬への適用Pointwise各馬のスコア（勝率や着順）を個別に回帰・分類する（MSE, Cross Entropy）。実装が容易。標準的なライブラリで対応可能。レース内の相互関係を無視する。1位の予測精度が埋もれがち。精度に限界あり。Pairwise2頭の馬のペアについて、どちらが先着するかを分類する（RankNet, LambdaRank）。順序関係を直接学習できる。ペア数が$O(N^2)$で増大し、計算コストが高い。有効だが計算重い。Listwiseレース全体の順位リストを一つのインスタンスとして学習する（ListNet, ListMLE）。レース全体の順列確率を最適化するため、最も競馬の構造に適している。実装が複雑。最適。

5.2. ListMLEとLambdaRankの適用単勝予測においては、1着（Top-1）の予測精度が全てである。2着以下の順位の正確さは、単勝馬券においては無価値である（三連単などを除く）。したがって、トップランクの予測に重きを置く損失関数が必要である。

5.2.1. ListMLE (Listwise Maximum Likelihood Estimation)ListMLEは、正解の順列（ランキング）が得られる確率（尤度）を最大化する手法である。Plackett-Luceモデルに基づき、次のように定義される 373839。$$L(\theta) = - \sum_{i=1}^{N} \log P(y_{(i)} | x_{(i)}, \theta)$$ここで、$y_{(i)}$は正解順位が$i$位の馬、$x_{(i)}$はその特徴量である。$P(y_{(i)} | \dots)$は、まだ順位が決まっていない残りの馬集合の中で、その馬が選ばれる確率（Softmax）である。$$P(y_{(i)} | \{y_{(i)}, \dots, y_{(N)}\}) = \frac{\exp(f(x_{(i)}))}{\sum_{k=i}^{N} \exp(f(x_{(k)}))}$$この損失関数は、特に1位（$i=1$）の項が損失全体に大きく寄与するため、1着馬を正しく1位として予測する能力（Top-1 Accuracy）を高める効果がある。5.2.2. LambdaRank / LambdaMARTLambdaRankは、NDCG（Normalized Discounted Cumulative Gain）などの評価指標を直接最適化するように勾配（Lambda）を定義する手法である。LambdaMARTはこの勾配ブースティング決定木（GBDT）版である 404141。単勝予測に特化する場合、NDCG@1 を最大化するようにLambdaを設定する。つまり、1位の馬が下位にランクされた場合に極めて大きな勾配（ペナルティ）を与え、下位の馬同士の順位の入れ替わりにはほとんど関心を示さないように学習させる 4241。5.3. Focal Lossによるキャリブレーションと学習の安定化競馬データは不均衡である（勝つ馬は1頭だけで、他は全て負け）。また、圧倒的な実力差があるレース（Easy Sample）と、大混戦のレース（Hard Sample）が混在している。Focal Lossは、モデルが容易に正解できるサンプルの重みを下げ、予測が困難なサンプル（Hard Sample）に重点的に学習リソースを割り当てる 4344。$$FL(p_t) = - \alpha_t (1 - p_t)^\gamma \log(p_t)$$ここで、$p_t$はモデルの予測確率、$\gamma$はフォーカスパラメータである。$\gamma > 0$ とすることで、自信満々に正解している（$p_t \approx 1$）データの損失をゼロに近づけ、境界線上のデータの学習を促進する。さらに、Focal Lossはモデルの**確率較正（Calibration）**を改善する副次的効果があることが報告されている。Cross Entropy損失はモデルを自信過剰（Overconfident）にしがちだが、Focal Lossは正解確率を極端に1に近づける圧力を緩和するため、出力される確率が真の発生確率に近づく（Calibrated）傾向がある 4445。これは、後述するケリー基準による資金管理において極めて重要である。

5.4. SoftRank: 微分可能なソート順位そのものを直接最適化したい場合、ソート操作は微分不可能（離散的）であるため、通常の誤差逆伝播法が使えない。しかし、「SoftSort」や「SoftRank」と呼ばれる微分可能な緩和手法を用いることで、順位をニューラルネットワークの出力として扱い、End-to-Endで学習することが可能になる 4647。これにより、着順という順序情報をロスなくモデルに取り込むことができる。6. 確率較正とベッティング戦略：エッジを利益に変える高精度な予測モデルが完成しても、それだけでは利益は保証されない。モデルが出力するスコアを「信頼できる確率」に変換し（Calibration）、その確率とオッズに基づいて「期待値がプラスの賭け」のみを選択し、適切な金額を投じる（Money Management）必要がある。6.1. 確率較正（Probability Calibration）AIモデル（特に深層学習やGBDT）が出力するスコアは、大小関係（ランキング）は正しくても、確率値としては歪んでいることが多い（例：勝率30%の馬に対して0.8というスコアを出す）。ROI計算には正確な確率が必須であるため、事後的な補正（Calibration）を行う 484950。

6.1.1. Isotonic Regression vs Platt ScalingPlatt Scaling (Sigmoid Calibration): モデルの出力をロジスティック回帰に通して確率に変換する。出力分布がS字カーブ（Sigmoid）に従うと仮定できる場合（SVMなど）に有効だが、複雑な分布には適さない 5152。Isotonic Regression (保順回帰): 単調増加性を保ちながら、ノンパラメトリックに関数を適合させる。S字カーブに従わない任意の歪みを補正できるため、競馬のような複雑なデータセットには、データ量が十分にある限りIsotonic Regressionの方が適している場合が多い 5153。

6.1.2. Brier Scoreによる評価キャリブレーションの良し悪しを測る指標として、Brier Scoreを用いる。$$BS = \frac{1}{N} \sum_{t=1}^{N} (f_t - o_t)^2$$ここで$f_t$は予測確率、$o_t$は実際の結果（1または0）。Brier Scoreは、Reliability（信頼性）、Resolution（解像度）、**Uncertainty（不確実性）**の3成分に分解できる 5455。Reliability: 予測確率と実際の発生頻度のズレ。これを最小化することがキャリブレーションの目標である。Resolution: 予測がいかに結果を分離できているか（0.5ばかり出力するモデルはResolutionが低い）。単勝モデルにおいては、単に的中率（Resolution）を追うだけでなく、Reliabilityを徹底的に改善することで、オッズとの乖離（エッジ）を正確に見積もることが可能となる。6.2. ケリー基準（Kelly Criterion）による資金管理期待値がプラスの賭けにおいて、破産リスクを抑えつつ資産成長率を最大化する最適な賭け金比率を決定する公式がケリー基準である 5657。$$f^* = \frac{bp - q}{b} = \frac{p(b+1) - 1}{b}$$ここで：$f^*$: 資金に対する賭け金の比率（Kelly Fraction）$b$: 純オッズ（Decimal Odds - 1）$p$: 推定勝率$q$: 敗率 ($1-p$)

6.2.1. Fractional Kellyとリスク管理理論上の $f^*$ は、推定勝率 $p$ が正確であることを前提としている。しかし、モデルの予測には必ず誤差（Uncertainty）が含まれる。勝率を過大評価していた場合、フル・ケリー（Full Kelly）で賭けると資産を急速に失うリスクがある。そのため、実務的には算出された $f^*$ の一部（例えば1/4や1/2）だけを賭ける「Fractional Kelly」戦略が推奨される 5856。$$f_{bet} = \lambda f^* \quad (0 < \lambda < 1)$$一般的に $\lambda = 0.2 \sim 0.3$ （クォーター・ケリー）あたりが、ボラティリティを抑えつつ長期的な成長を確保するスイートスポットとされる。

6.3. 期待値（EV）閾値の最適化全てのレースに賭ける必要はない。モデルが算出する期待値（EV）が一定の閾値を超えた場合のみエントリーする「選択的予測（Selective Prediction）」を行う 59。$$EV = p \times \text{Decimal Odds}$$$$\text{Bet if } EV > 1 + \alpha$$ここで $\alpha$ は要求するエッジ（例えば0.05なら期待値105%以上）。この閾値を検証データセット（Backtest）上でシミュレーションし、シャープレシオや最大ドローダウンを考慮して最適化する 6061。

7. 実装と結論

7.1. 推奨されるシステムパイプライン以上の議論を総合すると、ROI最大化のための最強のシステム構成は以下のようになる。Data Ingestion: スクレイピングによるレース結果、通過順、オッズの収集。Feature Engineering:通過順の正規化と変化率計算。Brisnet準拠のペース指数（E1, E2, LP）とターンタイム計算。ローリング平均によるトラックバイアス指数の動的生成。PyTorch Embedding層によるカテゴリ変数のベクトル化。Model Architecture:各馬の履歴をTemporal Transformerでエンコード。レース全体の相互作用をSet Transformerでモデリング。Loss Function: ListMLE または LambdaRank (NDCG@1) を採用し、1着予測に特化。Focal Lossを導入してキャリブレーションを向上。Post-Processing: Isotonic Regressionによる確率較正。Betting Strategy: Fractional Kelly Criterion (CP=0.25) と EV Thresholding を組み合わせた自動入札。

7.2. 結論単勝回収率の最大化は、単なる「当たり屋」になることではない。それは、不確実な事象に対して、統計的・数理的な優位性（Edge）を確立し、それを規律ある資金管理によって利益に変換する工学的プロセスである。本レポートで示したように、スクレイピングデータという「ありふれた鉱脈」も、通過順のベクトル解析やトラックバイアスの動的抽出といった高度な精製技術を通すことで、市場を出し抜くための「純金」となり得る。さらに、Set Transformerのような最新の深層学習アーキテクチャと、ListMLEやFocal Lossといった洗練された損失関数を組み合わせることで、従来の手法では捉えきれなかったレースのダイナミズムをモデル化できる。最終的に勝敗を分けるのは、モデルの精度（Accuracy）ではなく、モデル自身の不確実性を理解し、適切なリスクテイクを行う能力（Calibration & Management）である。この包括的なフレームワークを実装し、継続的に改善していくことが、競馬市場という巨大な不確実性の中で長期的な利益を上げ続ける唯一の道である。

補遺：技術的実装詳細A. ターンタイム計算のPython実装例Pythondef calculate_turn_time(distance, fractional_times, final_time):
    """
    6ハロン（約1200m）戦などを想定したターンタイム計算ロジックの概念実装。
    Brisnetのロジックに基づき、第2コール（4F地点）からストレッチコールまでの時間を算出する。
    """
    # データ構造例: fractional_times = {'2f': 22.5, '4f': 45.0, 'stretch': 57.0}
    
    if '4f' in fractional_times and 'stretch' in fractional_times:
        # 4ハロン地点からストレッチ（直線入口）までの区間タイム
        # この区間が概ねコーナー（ターン）に相当する場合が多い（距離設定による）
        turn_segment_time = fractional_times['stretch'] - fractional_times['4f']
        return turn_segment_time
    else:
        return None
B. 通過順正規化ロジックPythondef normalize_passing_order(order_list, field_size):
    """
    通過順リスト（例: ）をフィールドサイズ（例: 16）で正規化する。
    """
    if field_size <= 1:
        return [0.0] * len(order_list)
    
    normalized = [(p - 1) / (field_size - 1) for p in order_list]
    return normalized
C. PyTorchによるListMLE損失関数の概念実装Pythonimport torch
import torch.nn as nn
import torch.nn.functional as F

class ListMLELoss(nn.Module):
    def __init__(self):
        super(ListMLELoss, self).__init__()

    def forward(self, scores, target_ranks):
        """
        scores: (Batch, N_Runners) - モデルが出力した各馬のスコア（Logits）
        target_ranks: (Batch, N_Runners) - 正解の着順（0が1着、1が2着...）
        
        注意: ListMLEの実装では、正解順にスコアを並び替え、その尤度を計算する必要がある。
        """
        batch_size, n_runners = scores.shape
        loss = 0
        
        for i in range(batch_size):
            # 1. 正解の着順に従ってスコアをソートする
            # target_ranks[i] の値に基づいて scores[i] を並べ替える
            # 例: targetがなら、(馬1:3着, 馬2:1着, 馬3:2着) -> 並び替え後: [馬2スコア, 馬3スコア, 馬1スコア]
            _, sorted_indices = torch.sort(target_ranks[i])
            sorted_scores = scores[i][sorted_indices]
            
            # 2. 尤度の計算 (Plackett-Luce Model)
            # P(1位) * P(2位|1位除外) *...
            # log P = log(exp(s_1) / sum(exp(s_k))) = s_1 - log(sum(exp(s_k)))
            
            # 累積LogSumExpを使って効率的に計算
            # cumsum_exp = torch.flip(torch.cumsum(torch.flip(torch.exp(sorted_scores), ), 0), )
            # log_likelihood = sorted_scores - torch.log(cumsum_exp)
            
            # 簡易的なTop-1最大化のみを考えるなら、1位の項だけを計算しても良いが、
            # ListMLEは全順列を考慮する。
            
            # ここでは安定計算のためLogSumExpを使用
            for j in range(n_runners):
                # j番目に正しい馬のスコア
                s_j = sorted_scores[j]
                # j番目以降の馬（まだ順位が決まっていない馬）のスコアのLogSumExp
                lse_j = torch.logsumexp(sorted_scores[j:], dim=0)
                loss += -(s_j - lse_j)
                
        return loss / batch_size