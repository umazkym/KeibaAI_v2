# 08_シミュレーションと最適化

**最終更新日**: 2025-11-19

---

## 📑 目次

1. [モンテカルロシミュレーション](#モンテカルロシミュレーション)
2. [ポートフォリオ最適化](#ポートフォリオ最適化)
3. [実装詳細](#実装詳細)
4. [パフォーマンスチューニング](#パフォーマンスチューニング)

---

## 🎲 シミュレーションと確率推定

KeibaAI_v2は、モデルのタイプに応じて2つのアプローチを使い分けます。

### 1. System A: 確率ベース (v2.6 Main)

v2.6モデルは LightGBM の Binary Classification により、各馬の**1着確率 ($P_{win}$)** を直接出力します。

*   **入力**: 特徴量のみ
*   **出力**: `pred_prob` (0.0 ~ 1.0)
*   **シミュレーション**: 不要（モデル出力確率をそのまま使用、または期待値フィルタリングに使用）
*   **メリット**: 高速、解釈が容易

### 2. System B: 分布ベース (Experimental)

System B（μ/σ/νモデル）では、完走時間の確率分布を推定し、モンテカルロシミュレーションを行います。
**（以下はSystem Bに関する記述です）** 

### モンテカルロシミュレーション (System B)

### アルゴリズム

```python
for k = 1 to K (K=1000):
    for horse_i in race:
        # t分布からサンプリング
        time_i_k ~ t_ν(μ_i, σ_i²)

    # 着順決定
    finish_order = argsort(times)

    # 勝者記録
    winner_counts[finish_order[0]] += 1

# 勝率推定
P(win) = winner_counts / K
```

### 実装（RaceSimulator）

**ファイル**: `keibaai/src/sim/simulator.py`

```python
import numpy as np
from scipy.stats import t

class RaceSimulator:
    def __init__(self, K=1000):
        """
        Args:
            K: シミュレーション回数
        """
        self.K = K

    def simulate(
        self,
        mu_array: np.ndarray,
        sigma_array: np.ndarray,
        nu: float
    ) -> dict:
        """
        モンテカルロシミュレーション

        Args:
            mu_array: 各馬の期待完走時間 [N]
            sigma_array: 各馬の標準偏差 [N]
            nu: レース全体の自由度

        Returns:
            dict: {
                'win_probs': [N],
                'place_probs': [N],
                'show_probs': [N]
            }
        """
        N = len(mu_array)
        win_counts = np.zeros(N)
        place_counts = np.zeros(N)  # 2着以内
        show_counts = np.zeros(N)   # 3着以内

        for k in range(self.K):
            # 各馬の完走時間サンプリング
            finish_times = np.array([
                t.rvs(df=nu, loc=mu_i, scale=sigma_i)
                for mu_i, sigma_i in zip(mu_array, sigma_array)
            ])

            # 着順決定
            finish_order = np.argsort(finish_times)

            # カウント
            win_counts[finish_order[0]] += 1
            place_counts[finish_order[:2]] += 1
            show_counts[finish_order[:3]] += 1

        # 確率推定
        win_probs = win_counts / self.K
        place_probs = place_counts / self.K
        show_probs = show_counts / self.K

        return {
            'win_probs': win_probs,
            'place_probs': place_probs,
            'show_probs': show_probs
        }
```

### 高速化版（NumPyベクトル化）

```python
def simulate_vectorized(
    self,
    mu_array: np.ndarray,
    sigma_array: np.ndarray,
    nu: float
) -> dict:
    """ベクトル化版（10-20x高速）"""

    N = len(mu_array)

    # 一度に全サンプル生成 [K x N]
    finish_times_all = np.array([
        t.rvs(df=nu, loc=mu_array, scale=sigma_array, size=self.K)
        for _ in range(N)
    ]).T

    # 着順決定 [K x N]
    finish_orders = np.argsort(finish_times_all, axis=1)

    # カウント
    win_counts = np.bincount(finish_orders[:, 0], minlength=N)
    place_counts = np.bincount(finish_orders[:, :2].flatten(), minlength=N)
    show_counts = np.bincount(finish_orders[:, :3].flatten(), minlength=N)

    win_probs = win_counts / self.K
    place_probs = place_counts / self.K
    show_probs = show_counts / self.K

    return {
        'win_probs': win_probs,
        'place_probs': place_probs,
        'show_probs': show_probs
    }
```

### シミュレーション結果の保存

```json
{
  "race_id": "202310010811",
  "race_date": "2023-10-01",
  "K": 1000,
  "nu": 5.2,
  "horses": [
    {
      "horse_number": 1,
      "horse_name": "サンプルホース1",
      "mu": 72.3,
      "sigma": 1.2,
      "win_prob": 0.352,
      "place_prob": 0.681,
      "show_prob": 0.854
    },
    {
      "horse_number": 2,
      "horse_name": "サンプルホース2",
      "mu": 73.1,
      "sigma": 2.1,
      "win_prob": 0.118,
      "place_prob": 0.384,
      "show_prob": 0.602
    },
    ...
  ]
}
```

---

## 💰 ポートフォリオ最適化

### Kelly Criterion（ケリー基準）

**発明者**: John L. Kelly Jr. (1956)

**基本公式**:
```
f* = (p * (o - 1) - (1 - p)) / (o - 1)

where:
  f*: 資産の何割を賭けるか
  p:  勝率
  o:  オッズ
```

**例**:
```python
p = 0.35  # 35%の勝率
o = 3.2   # 3.2倍のオッズ

f_star = (0.35 * (3.2 - 1) - (1 - 0.35)) / (3.2 - 1)
       = (0.35 * 2.2 - 0.65) / 2.2
       = (0.77 - 0.65) / 2.2
       = 0.055  # → 資産の5.5%を賭ける
```

### Fractional Kelly（リスク調整版）

**問題**: Full Kellyは積極的すぎる → ドローダウンが大きい

**解決**: Kelly fractionで縮小

```python
f_actual = f_star * kelly_fraction

# kelly_fraction の推奨値:
# 0.25: 保守的（推奨）
# 0.50: 標準
# 1.00: フルケリー（リスク大）
```

### Multi-Horse Portfolio Optimization

**最適化問題**:
```
Maximize: Σ f_i * log(1 + (o_i - 1) * f_i / W_0)

Subject to:
  Σ f_i ≤ W_0          # 資本制約
  f_i ≥ 0              # 非負制約
  EV_i > threshold     # 期待値フィルタ

where:
  f_i: 馬iへの賭け金
  o_i: 馬iのオッズ
  W_0: 総資本
  EV_i = p_i * o_i - 1  # 期待値
```

### 2段階投資ロジック (Binary & Simulation)

System A (Binary) と System B (Simulation) の両方に対応する投資ロジックです。

**基本入力**:
*   **勝率 ($p$)**: System Aなら `pred_prob`、System Bならモンテカルロ結果の `win_prob`
*   **オッズ ($o$)**: 直前オッズ

**フェーズ1: 朝一の予算配分**
*   **入力**: 全レースの予測結果（JSON）
*   **ロジック**: 期待値 $E = p \times o - 1$ が正のレースに予算を配分。

**フェーズ2: レース直前の投資実行**
*   **タイミング**: 各レース発走5分前
*   **ロジック**: Fractional Kelly Criterion で賭け金を決定。
    *   $f^* = \frac{p(o-1) - (1-p)}{o-1} \times \text{fraction}$
    *   $f^* \le 0$ なら見送り（SKIP）

---

## 💻 実装詳細

### 日次予算配分クラス

**ファイル**: `keibaai/src/optimizer/daily_allocator.py`

```python
class DailyAllocator:
    def allocate_budget(self, daily_simulations, daily_odds, total_daily_budget):
        """
        1. 各レースのスコア（期待成長率）を計算
        2. スコアが正のレースに予算を比例配分
        """
        ...
```

### 日次最適化スクリプト (統合版)

**ファイル**: `keibaai/src/optimizer/optimize_daily_races.py`

```python
def main():
    # 1. 全データのロード
    simulations = load_simulation_data(...)
    daily_odds = load_odds_data(...) # 朝一オッズ

    # 2. 予算配分 (Phase 1)
    allocator = DailyAllocator(config)
    race_budgets = allocator.allocate_budget(simulations, daily_odds, args.W_0)

    # 3. レースごとの実行 (Phase 2)
    optimizer = PortfolioOptimizer(config)
    
    for sim in simulations:
        race_id = sim['race_id']
        budget = race_budgets.get(race_id, 0)
        
        if budget > 0:
            # 直前オッズを取得（本番ではここで再取得）
            current_odds = get_latest_odds(race_id)
            
            # 最適化実行
            result = optimizer.optimize(sim, current_odds, W_0=budget)
            save_order(result)
```

---

## ⚡ パフォーマンスチューニング

### ベンチマーク

| K | 実装 | 時間（秒/レース） | 倍速 |
|---|------|-----------------|------|
| 1,000 | Pythonループ | 2.5 | 1x |
| 1,000 | NumPyベクトル化 | 0.15 | 16.7x |
| 10,000 | NumPyベクトル化 | 1.2 | - |

### 並列化（複数レース）

```python
from multiprocessing import Pool

def simulate_single_race(args):
    race_id, mu, sigma, nu = args
    simulator = RaceSimulator(K=1000)
    return race_id, simulator.simulate(mu, sigma, nu)

# 並列実行
with Pool(processes=4) as pool:
    results = pool.map(simulate_single_race, race_args_list)
```

### キャッシング

```python
from functools import lru_cache

@lru_cache(maxsize=100)
def simulate_cached(mu_tuple, sigma_tuple, nu, K):
    """タプルは hashable なのでキャッシュ可能"""
    mu = np.array(mu_tuple)
    sigma = np.array(sigma_tuple)
    simulator = RaceSimulator(K)
    return simulator.simulate(mu, sigma, nu)
```

---

**次のドキュメント**: [09_設定とテスト.md](./09_設定とテスト.md)
