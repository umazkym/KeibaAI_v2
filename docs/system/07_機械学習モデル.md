# 07_機械学習モデル

**最終更新日**: 2025-11-24  
**改善方針**: μモデル優先改善 → σ・ν再学習（詳細は [13_モデル改善戦略.md](./13_モデル改善戦略.md) を参照）

> [!WARNING]
> **データリーク警告**: 過去のνモデルで`win_odds`が特徴量に含まれていました。新規モデルは必ず [14_モデル命名規則とベストプラクティス.md](./14_モデル命名規則とベストプラクティス.md) のチェックリストに従ってください。

---

## 📑 目次

1. [モデル概要](#モデル概要)
2. [3パラメータモデル（μ, σ, ν）](#3パラメータモデル)
3. [LightGBM設定](#lightgbm設定)
4. [確率校正](#確率校正)
5. [訓練フロー](#訓練フロー)
6. [評価指標](#評価指標)

---

## 🔄 モデル開発状況（2系統並行）

KeibaAI_v2は**2つのモデル系統**を並行開発しています：

### System A: μv2.4（1着予測・ROI特化）

**目的**: 単勝的中率とROI向上（過剰人気回避）
**手法**: LightGBM Binary Classification (Selected Features)
**状態**: 運用可能、ROI最大化フェーズ

| 項目 | 現状 (v2.4) | 次の目標 |
|------|------|----------|
| **ROI** | **78.75%** | 85.0%+ |
| **的中率** | 27.82% | 維持 |
| **平均配当** | - | 5.0倍+ |
| **改善方針** | ROI主導チューニング | オッズ加重学習 |

**特徴**:
- リーク完全排除（8種類）
- **大衆化バイアス排除**（生産者、近走成績、騎手相性を削除）
- AUC 0.7787

**今後の方向性**:
**今後の方向性**:
1. **ROI主導型チューニング** (Optuna metric=roi)
2. **オッズ加重学習** (Odds-Weighted Training)
3. EV（期待値）ベースのベット選択

### System B: μ/σ/νモデル（時間予測 + 確率分布）

**目的**: 完走時間予測、不確実性の定量化  
**手法**: Regression + t分布モデリング  
**状態**: 開発予定（μv2.0承認後）

| コンポーネント | 状態 | 説明 |
|--------------|------|------|
| **μモデル（時間予測）** | 未着手 | 完走時間の予測（回帰問題） |
| **σモデル** | 旧版あり | μの残差ベースで再学習予定 |
| **νモデル** | 旧版あり | リーク排除版を再開発予定 |

**開発計画**:
```
Phase 1: 時間予測μモデル開発（2-3週間）
  ├─ データ準備（μv2.0の基盤を活用）
  ├─ 目的変数: finish_time_seconds
  ├─ リーク完全排除
  └─ 評価: RMSE, Spearman相関

Phase 2: σモデル再学習（1週間）
  └─ 新μの残差ベースで学習

Phase 3: νモデル再学習（1週間）
  └─ リーク排除版

Phase 4: 統合評価
  └─ μ/σ/νシステムのROI測定
```

### 2つのシステムの使い分け

| 用途 | 使用モデル | 理由 |
|------|-----------|------|
| **単勝ベット** | μv2.0 | ROI最適化済み |
| **馬連・三連複** | μ/σ/ν | 複数馬の相対順位予測 |
| **確率分析** | μ/σ/ν | 不確実性の定量化 |
| **シミュレーション** | μ/σ/ν | モンテカルロ法 |

> [!NOTE]
> 2つのシステムは相補的であり、用途に応じて使い分けることで最大の効果を発揮します。

---

## 🎯 モデル概要

### 予測目標

従来の着順予測（分類問題）ではなく、**確率分布の推定**：

```python
# ❌ 従来の分類モデル
y_pred = [1, 5, 3, 2, ...]  # 各馬の予測着順

# ✅ KeibaAI_v2の確率モデル
μ = [72.3, 73.1, 71.8, ...]  # 期待完走時間（秒）
σ = [1.2, 2.5, 0.8, ...]     # 馬固有の不確実性（秒）
ν = 5.0                      # レース全体の混沌度（自由度）

# → t分布でモデル化
# → モンテカルロで勝率推定
P(win) = [0.35, 0.12, 0.42, ...]
```

---

## 🔬 3パラメータモデル（μ, σ, ν）

### 数学的背景

各馬iの完走時間を**t分布**でモデル化：

```
T_i ~ t_ν(μ_i, σ_i²)

where:
  μ_i: 馬iの期待完走時間（location parameter）
  σ_i: 馬iの標準偏差（scale parameter）
  ν:   レース全体の自由度（degrees of freedom）
```

**t分布を選ぶ理由**:
1. 外れ値に頑健（heavy-tailed）
2. νが小さい → 予想外の展開が起きやすい（荒れるレース）
3. νが大きい → 正規分布に近づく（堅いレース）

### パラメータの解釈

| パラメータ | 意味 | 典型値 | 影響要因 |
|-----------|------|-------|---------|
| **μ（mu）** | 期待完走時間 | 70-150秒 | 距離、馬の能力、騎手 |
| **σ（sigma）** | 馬固有の不確実性 | 0.5-3.0秒 | 馬の安定性、経験 |
| **ν（nu）** | レース全体の混沌度 | 3-10 | レースクラス、馬場状態 |

---

### μモデル（期待完走時間）

**ファイル**: `keibaai/src/modules/models/model_train.py`

#### アプローチ

**2段階学習**:
1. **Ranking**: 相対的な強さを学習（LambdaRank）
2. **Regression**: 絶対的なタイムを学習（RMSE最小化）

```python
import numpy as np
from lightgbm import LGBMRanker, LGBMRegressor

class MuEstimator:
    def __init__(self, ranker_params=None, regressor_params=None):
        # Stage 1: Ranking（相対的な強さを学習）
        self.ranker_params = ranker_params or {
            'objective': 'lambdarank',
            'metric': 'ndcg',
            'n_estimators': 1000,
            'learning_rate': 0.01,
            'num_leaves': 31,
            'verbose': -1
        }
        
        # Stage 2: Regression（絶対的なタイムを学習）
        self.regressor_params = regressor_params or {
            'objective': 'regression',
            'metric': 'rmse',
            'n_estimators': 1000,
            'learning_rate': 0.01,
            'num_leaves': 31,
            'verbose': -1
        }
        
        self.ranker = None
        self.regressor = None

    def fit(self, X, y, group):
        """
        Args:
            X: 特徴量 [N x D]
            y: 完走時間（秒） [N]
            group: レースごとの馬数 [num_races]
        """
        # Stage 1: Rankerの学習
        # yは完走時間（浮動小数点）なので、整数のrelevanceスコアに変換
        rank_target = []
        start_idx = 0
        for g in group:
            group_y = y[start_idx:start_idx+g]
            # タイムからランクを計算（0-indexed）
            ranks = np.argsort(np.argsort(group_y))
            # relevance: 最大ランク - 現在のランク（1着が最高スコア）
            relevance = (g - 1) - ranks
            rank_target.extend(relevance)
            start_idx += g
        rank_target = np.array(rank_target, dtype=int)
        
        # Rankerの学習
        self.ranker = LGBMRanker(**self.ranker_params)
        self.ranker.fit(X, rank_target, group=group)
        rank_scores = self.ranker.predict(X)

        # Stage 2: Regressorの学習
        X_aug = X.copy()
        X_aug['rank_score'] = rank_scores  # Rankerのスコアを特徴量に追加
        
        self.regressor = LGBMRegressor(**self.regressor_params)
        self.regressor.fit(X_aug, y)

    def predict(self, X):
        rank_scores = self.ranker.predict(X)
        X_aug = X.copy()
        X_aug['rank_score'] = rank_scores
        mu = self.regressor.predict(X_aug)
        return mu
```

**重要な実装ポイント**:
- `LGBMRanker`は整数型のrelevanceスコアを要求するため、完走時間（浮動小数点）から各レース内でランクを計算
- `(最大ランク - 現在のランク)`で1着が最高スコアになるように変換
- Rankerのスコアを特徴量に追加することで、相対的な強さと絶対的なタイムの両方を学習

---

### σモデル（分散推定）

**ファイル**: `keibaai/src/modules/models/sigma_estimator.py`（実装済み）

> **Note**: Muモデルの残差から各馬の不確実性を学習するLightGBMモデルを実装済みです。

#### アプローチ

**残差の分散を学習**:

```python
# μモデルで予測
mu_pred = mu_model.predict(X)

# 実際の残差
residuals = y_true - mu_pred

# 残差の絶対値を学習（σの代理変数）
sigma_model = LGBMRegressor(
    objective='regression',
    metric='rmse'
)
sigma_model.fit(X, np.abs(residuals))

# σの予測
sigma_pred = sigma_model.predict(X_test)
```

**特徴量の工夫**:
- 馬の過去成績のばらつき
- 騎手の安定性
- 経験レース数（少ないほど不確実）

---

### νモデル（混沌度推定）

**ファイル**: `keibaai/src/modules/models/nu_estimator.py`（実装済み）

> [!CAUTION]
> **データリーク発見**: 過去のνモデルで`avg_win_odds`, `std_win_odds`が特徴量に含まれていました。これらは**最終オッズ（未来情報）**であり、実運用では使用不可能です。新規モデルでは必ず除外してください。

> **Note**: レースレベルの特徴量からMLE推定したνを学習するLightGBMモデルを実装済みです。

#### アプローチ

**レースレベルの特徴量から推定**:

```python
# レースごとに1つのν
race_features = [
    'head_count',        # 出走頭数（多い → 混沌）
    'race_class',        # クラス（低い → 混沌）
    'track_condition',   # 馬場状態（悪い → 混沌）
    'prize_1st',         # 賞金（低い → 混沌）
]

nu_model = LGBMRegressor(
    objective='regression',
    metric='mae'
)
nu_model.fit(race_features, nu_true)
```

**νの真値の計算**（訓練データから）:
```python
from scipy.stats import t

def estimate_nu_from_residuals(residuals):
    """
    残差からνをMLE推定
    """
    def neg_log_likelihood(nu):
        return -np.sum(t.logpdf(residuals, df=nu))

    result = minimize_scalar(
        neg_log_likelihood,
        bounds=(2, 30),
        method='bounded'
    )
    return result.x
```

---

## ⚙️ LightGBM設定

### ハイパーパラメータ

**μモデル（Ranker）**:
```yaml
# configs/models.yaml
models:
  lgbm_ranker:
    objective: lambdarank
    metric: ndcg
    boosting_type: gbdt
    n_estimators: 2000
    learning_rate: 0.01
    num_leaves: 31
    max_depth: -1
    feature_fraction: 0.8
    bagging_fraction: 0.8
    bagging_freq: 5
    lambda_l1: 0.0
    lambda_l2: 0.0
    min_data_in_leaf: 20
    verbose: -1
```

**μモデル（Regressor）**:
```yaml
  lgbm_regressor:
    objective: regression
    metric: rmse
    boosting_type: gbdt
    n_estimators: 2000
    learning_rate: 0.01
    num_leaves: 31
    max_depth: -1
    feature_fraction: 0.8
    bagging_fraction: 0.8
    bagging_freq: 5
    lambda_l1: 0.0
    lambda_l2: 0.0
    min_data_in_leaf: 20
    verbose: -1
```

### 学習率スケジューリング

```python
callbacks = [
    lgb.early_stopping(stopping_rounds=50),
    lgb.log_evaluation(period=100),
]

model = lgb.train(
    params,
    train_data,
    num_boost_round=2000,
    valid_sets=[valid_data],
    callbacks=callbacks
)
```

---

## 📊 確率校正

### Temperature Scaling

**問題**: LightGBMの生出力は確率として校正されていない

**解決**: Temperature Scaling

```python
from sklearn.calibration import CalibratedClassifierCV

# μ, σ, ν → 勝率への変換（シミュレーション後）
win_probs_uncalibrated = simulate_race(mu, sigma, nu)

# Isotonic Regressionで校正
calibrator = CalibratedClassifierCV(
    base_estimator=None,  # すでに確率が出ている
    method='isotonic',    # or 'sigmoid'
    cv='prefit'           # 事前に訓練済み
)

calibrator.fit(win_probs_uncalibrated.reshape(-1, 1), y_true)

# 校正済み確率
win_probs_calibrated = calibrator.predict_proba(
    win_probs_uncalibrated.reshape(-1, 1)
)[:, 1]
```

### Platt Scaling（代替手法）

```python
from sklearn.linear_model import LogisticRegression

# ロジスティック回帰で校正
platt_scaler = LogisticRegression()
platt_scaler.fit(
    win_probs_uncalibrated.reshape(-1, 1),
    y_true
)

win_probs_calibrated = platt_scaler.predict_proba(
    win_probs_uncalibrated.reshape(-1, 1)
)[:, 1]
```

---

## 🏋️ 訓練フロー

### 時系列分割

```python
from sklearn.model_selection import TimeSeriesSplit

# ❌ NG: ランダム分割（データリーク）
from sklearn.model_selection import KFold
kfold = KFold(n_splits=5, shuffle=True)

# ✅ OK: 時系列分割
tscv = TimeSeriesSplit(n_splits=5)

for train_idx, valid_idx in tscv.split(X):
    X_train, X_valid = X.iloc[train_idx], X.iloc[valid_idx]
    y_train, y_valid = y.iloc[train_idx], y.iloc[valid_idx]

    # 訓練
    model.fit(X_train, y_train)

    # 検証
    y_pred = model.predict(X_valid)
```

### グループ化（レース単位）

```python
# レースごとにグループ化
groups = df.groupby('race_id').size().values

# LightGBM Rankerの訓練
ranker.fit(X, y, group=groups)
```

### 訓練スクリプト

```bash
# μモデルの訓練
python scripts/training/train_mu_model.py \
  --start_date 2020-01-01 \
  --end_date 2023-12-31 \
  --output_dir data/models/20231015_lgbm

# σ, νモデルの訓練
python scripts/training/train_sigma_nu_models.py \
  --mu_model_path data/models/20231015_lgbm/mu_model.txt \
  --output_dir data/models/20231015_lgbm
```

---

## 📏 評価指標

### 1. Brier Score（確率精度）

```python
from sklearn.metrics import brier_score_loss

# P(win)と実際の勝敗（0 or 1）
brier = brier_score_loss(y_true, win_probs)
# 0.0 (perfect) ~ 1.0 (worst)
```

**解釈**:
- Brier Score < 0.20: 優秀
- 0.20 - 0.25: 良好
- 0.25 - 0.30: 改善の余地あり

### 2. Expected Calibration Error (ECE)

```python
def calculate_ece(y_true, y_prob, n_bins=10):
    """
    Expected Calibration Error

    確率の予測値と実際の勝率のズレを測定
    """
    bins = np.linspace(0, 1, n_bins + 1)
    bin_indices = np.digitize(y_prob, bins) - 1

    ece = 0.0
    for i in range(n_bins):
        mask = bin_indices == i
        if mask.sum() > 0:
            avg_prob = y_prob[mask].mean()
            avg_true = y_true[mask].mean()
            ece += mask.sum() / len(y_true) * abs(avg_prob - avg_true)

    return ece
```

**解釈**:
- ECE < 0.05: 非常によく校正されている
- 0.05 - 0.10: 許容範囲
- > 0.10: 校正が必要

### 3. ROI（投資収益率）

```python
def calculate_roi(bets, outcomes, odds):
    """
    Return on Investment

    Args:
        bets: 賭け金配分 [N]
        outcomes: 結果（1=的中, 0=外れ） [N]
        odds: オッズ [N]

    Returns:
        ROI: 投資収益率
    """
    total_bet = bets.sum()
    total_return = (bets * outcomes * odds).sum()

    roi = total_return / total_bet
    return roi
```

**解釈**:
- ROI > 1.0: 利益
- ROI = 1.0: 損益分岐点
- ROI < 1.0: 損失

### 4. Hit Rate（的中率）

```python
# 1着予測の的中率
hit_rate = (predicted_winners == actual_winners).mean()
```

---

## 🔧 ハイパーパラメータ最適化（OptunaTuner）

**ファイル**: `keibaai/src/modules/models/optuna_tuner.py`

LightGBMモデルのハイパーパラメータを自動で最適化するためのOptunaベースのチューニングフレームワークです。

### アプローチ

**2つのチューニングモード**:
1. **Regressorチューニング**: 時系列クロスバリデーション（TimeSeriesSplit）
2. **Rankerチューニング**: グループ境界を考慮したホールドアウト法

```python
from keibaai.src.modules.models.optuna_tuner import OptunaTuner

# チューナーの初期化
tuner = OptunaTuner(n_trials=50, timeout=3600)

# Regressorのチューニング
best_params_reg = tuner.tune_lgbm_regressor(X, y)

# Rankerのチューニング
best_params_rank = tuner.tune_lgbm_ranker(X, rank_target, groups)
```

### 最適化されるハイパーパラメータ

| パラメータ | 探索範囲 | 説明 |
|-----------|---------|------|
| `lambda_l1` | 1e-8 ~ 10.0 (log) | L1正則化 |
| `lambda_l2` | 1e-8 ~ 10.0 (log) | L2正則化 |
| `num_leaves` | 2 ~ 256 | 葉の最大数 |
| `feature_fraction` | 0.4 ~ 1.0 | 特徴量サンプリング率 |
| `bagging_fraction` | 0.4 ~ 1.0 | データサンプリング率 |
| `bagging_freq` | 1 ~ 7 | バギング頻度 |
| `min_child_samples` | 5 ~ 100 | 葉ノードの最小サンプル数 |
| `learning_rate` | 0.001 ~ 0.1 (log) | 学習率 |

### クロスバリデーション戦略

**Regressor**: `TimeSeriesSplit(n_splits=3)`で時系列性を保持

**Ranker**: レース単位のグループを維持するため、最後の20%をホールドアウト
```python
# グループ境界で分割（レースを途中で分割しない）
adjusted_split_idx = group_cumsum[split_group_idx]
```

---

## 🏆 アンサンブル学習（StackingEnsemble）

**ファイル**: `keibaai/src/modules/models/stacking_ensemble.py`

複数のベースモデルの予測を組み合わせて、より頑健で高精度な予測を実現します。

### アーキテクチャ

```
Level 0 (Base Models):
┌──────────┐  ┌──────────┐  ┌──────────┐
│ Model 1  │  │ Model 2  │  │ Model 3  │
│(LightGBM)│  │(LightGBM)│  │(LightGBM)│
└─────┬────┘  └─────┬────┘  └─────┬────┘
      │             │             │
      └──────┬──────┴──────┬──────┘
             │  Out-of-Fold │
             │  Predictions │
             ▼             ▼
      ┌────────────────────────┐
      │ Level 1 (Meta Model)  │
      │  Ridge Regression     │
      └───────────┬────────────┘
                  │
                  ▼
           Final Prediction
```

### 使用方法

```python
from keibaai.src.modules.models.stacking_ensemble import StackingEnsemble
from lightgbm import LGBMRegressor

# ベースモデルの定義（異なる設定）
base_models = [
    LGBMRegressor(n_estimators=1000, learning_rate=0.01, num_leaves=31),
    LGBMRegressor(n_estimators=1500, learning_rate=0.005, num_leaves=63),
    LGBMRegressor(n_estimators=800, learning_rate=0.02, num_leaves=15)
]

# アンサンブルモデルの学習
ensemble = StackingEnsemble(base_models=base_models, n_folds=5)
ensemble.fit(X_train, y_train)

# 予測
y_pred = ensemble.predict(X_test)
```

### アンサンブルの利点

1. **分散の削減**: 複数モデルの平均化により過学習を抑制
2. **バイアスの補正**: メタモデルが各ベースモデルの弱点を補完
3. **頑健性の向上**: 単一モデルの失敗リスクを軽減

---

## 📊 統合トレーニングパイプライン

**ファイル**: `scripts/training/experimental/train_full_pipeline.py`

Mu, Sigma, Nu の3つのモデルを一括で学習し、ハイパーパラメータ最適化とアンサンブル学習を統合したパイプラインです。

### 実行コマンド

```bash
# 基本的な学習
python scripts/training/experimental/train_full_pipeline.py

# ハイパーパラメータチューニング有効化
python scripts/training/experimental/train_full_pipeline.py --tune

# アンサンブル学習有効化
python scripts/training/experimental/train_full_pipeline.py --ensemble

# 両方を有効化
python scripts/training/experimental/train_full_pipeline.py --tune --ensemble
```

### パイプラインの流れ

1. **データ読み込み**: `features.parquet`から特徴量を取得
2. **Muモデル学習**:
   - `--tune`が有効な場合、OptunaでRankerとRegressorのパラメータを最適化
   - `--ensemble`が有効な場合、StackingEnsembleで複数モデルを結合
3. **Sigmaモデル学習**: Muモデルの残差から各馬の不確実性を学習
4. **Nuモデル学習**: レース特徴量から混沌度を学習
5. **モデル保存**: `artifacts/models/`に保存

---

## 🎓 モデル改善のアイデア

### 1. アンサンブル（実装済み）

> **Note**: `StackingEnsemble`として実装済みです（上記参照）。

```python
# 複数モデルの平均
mu_ensemble = (mu_lgbm + mu_xgboost + mu_catboost) / 3
```

### 2. Stacking（実装済み）

> **Note**: `StackingEnsemble`クラスとして実装済みです（上記参照）。

```python
# Level 0: Base models
mu_lgbm = lgbm_model.predict(X)
mu_xgb = xgb_model.predict(X)

# Level 1: Meta model
X_meta = np.c_[mu_lgbm, mu_xgb]
meta_model = LinearRegression()
meta_model.fit(X_meta, y_true)

mu_final = meta_model.predict(X_meta_test)
```

### 3. ニューラルネットワーク（Future Enhancement）

```python
import tensorflow as tf

model = tf.keras.Sequential([
    total_return = (bets * outcomes * odds).sum()

    roi = total_return / total_bet
    return roi
```

**解釈**:
- ROI > 1.0: 利益
- ROI = 1.0: 損益分岐点
- ROI < 1.0: 損失

### 4. Hit Rate（的中率）

```python
# 1着予測の的中率
hit_rate = (predicted_winners == actual_winners).mean()
```

---

## 🔧 ハイパーパラメータ最適化（OptunaTuner）

**ファイル**: `keibaai/src/modules/models/optuna_tuner.py`

LightGBMモデルのハイパーパラメータを自動で最適化するためのOptunaベースのチューニングフレームワークです。

### アプローチ

**2つのチューニングモード**:
1. **Regressorチューニング**: 時系列クロスバリデーション（TimeSeriesSplit）
2. **Rankerチューニング**: グループ境界を考慮したホールドアウト法

```python
from keibaai.src.modules.models.optuna_tuner import OptunaTuner

# チューナーの初期化
tuner = OptunaTuner(n_trials=50, timeout=3600)

# Regressorのチューニング
best_params_reg = tuner.tune_lgbm_regressor(X, y)

# Rankerのチューニング
best_params_rank = tuner.tune_lgbm_ranker(X, rank_target, groups)
```

### 最適化されるハイパーパラメータ

| パラメータ | 探索範囲 | 説明 |
|-----------|---------|------|
| `lambda_l1` | 1e-8 ~ 10.0 (log) | L1正則化 |
| `lambda_l2` | 1e-8 ~ 10.0 (log) | L2正則化 |
| `num_leaves` | 2 ~ 256 | 葉の最大数 |
| `feature_fraction` | 0.4 ~ 1.0 | 特徴量サンプリング率 |
| `bagging_fraction` | 0.4 ~ 1.0 | データサンプリング率 |
| `bagging_freq` | 1 ~ 7 | バギング頻度 |
| `min_child_samples` | 5 ~ 100 | 葉ノードの最小サンプル数 |
| `learning_rate` | 0.001 ~ 0.1 (log) | 学習率 |

### クロスバリデーション戦略

**Regressor**: `TimeSeriesSplit(n_splits=3)`で時系列性を保持

**Ranker**: レース単位のグループを維持するため、最後の20%をホールドアウト
```python
# グループ境界で分割（レースを途中で分割しない）
adjusted_split_idx = group_cumsum[split_group_idx]
```

---

## 🏆 アンサンブル学習（StackingEnsemble）

**ファイル**: `keibaai/src/modules/models/stacking_ensemble.py`

複数のベースモデルの予測を組み合わせて、より頑健で高精度な予測を実現します。

### アーキテクチャ

```
Level 0 (Base Models):
┌──────────┐  ┌──────────┐  ┌──────────┐
│ Model 1  │  │ Model 2  │  │ Model 3  │
│(LightGBM)│  │(LightGBM)│  │(LightGBM)│
└─────┬────┘  └─────┬────┘  └─────┬────┘
      │             │             │
      └──────┬──────┴──────┬──────┘
             │  Out-of-Fold │
             │  Predictions │
             ▼             ▼
      ┌────────────────────────┐
      │ Level 1 (Meta Model)  │
      │  Ridge Regression     │
      └───────────┬────────────┘
                  │
                  ▼
           Final Prediction
```

### 使用方法

```python
from keibaai.src.modules.models.stacking_ensemble import StackingEnsemble
from lightgbm import LGBMRegressor

# ベースモデルの定義（異なる設定）
base_models = [
    LGBMRegressor(n_estimators=1000, learning_rate=0.01, num_leaves=31),
    LGBMRegressor(n_estimators=1500, learning_rate=0.005, num_leaves=63),
    LGBMRegressor(n_estimators=800, learning_rate=0.02, num_leaves=15)
]

# アンサンブルモデルの学習
ensemble = StackingEnsemble(base_models=base_models, n_folds=5)
ensemble.fit(X_train, y_train)

# 予測
y_pred = ensemble.predict(X_test)
```

### アンサンブルの利点

1. **分散の削減**: 複数モデルの平均化により過学習を抑制
2. **バイアスの補正**: メタモデルが各ベースモデルの弱点を補完
3. **頑健性の向上**: 単一モデルの失敗リスクを軽減

---

## 📊 統合トレーニングパイプライン

**ファイル**: `scripts/training/experimental/train_full_pipeline.py`

Mu, Sigma, Nu の3つのモデルを一括で学習し、ハイパーパラメータ最適化とアンサンブル学習を統合したパイプラインです。

### 実行コマンド

```bash
# 基本的な学習
python scripts/training/experimental/train_full_pipeline.py

# ハイパーパラメータチューニング有効化
python scripts/training/experimental/train_full_pipeline.py --tune

# アンサンブル学習有効化
python scripts/training/experimental/train_full_pipeline.py --ensemble

# 両方を有効化
python scripts/training/experimental/train_full_pipeline.py --tune --ensemble
```

### パイプラインの流れ

1. **データ読み込み**: `features.parquet`から特徴量を取得
2. **Muモデル学習**:
   - `--tune`が有効な場合、OptunaでRankerとRegressorのパラメータを最適化
   - `--ensemble`が有効な場合、StackingEnsembleで複数モデルを結合
3. **Sigmaモデル学習**: Muモデルの残差から各馬の不確実性を学習
4. **Nuモデル学習**: レース特徴量から混沌度を学習
5. **モデル保存**: `artifacts/models/`に保存

---

## 🎓 モデル改善のアイデア

### 1. アンサンブル（実装済み）

> **Note**: `StackingEnsemble`として実装済みです（上記参照）。

```python
# 複数モデルの平均
mu_ensemble = (mu_lgbm + mu_xgboost + mu_catboost) / 3
```

### 2. Stacking（実装済み）

> **Note**: `StackingEnsemble`クラスとして実装済みです（上記参照）。

```python
# Level 0: Base models
mu_lgbm = lgbm_model.predict(X)
mu_xgb = xgb_model.predict(X)

# Level 1: Meta model
X_meta = np.c_[mu_lgbm, mu_xgb]
meta_model = LinearRegression()
meta_model.fit(X_meta, y_true)

mu_final = meta_model.predict(X_meta_test)
```

### 3. ニューラルネットワーク（Future Enhancement）

```python
import tensorflow as tf

model = tf.keras.Sequential([
    tf.keras.layers.Dense(128, activation='relu'),
    tf.keras.layers.Dropout(0.3),
    tf.keras.layers.Dense(64, activation='relu'),
    tf.keras.layers.Dropout(0.3),
    tf.keras.layers.Dense(1, activation='linear')  # μの予測
])

model.compile(optimizer='adam', loss='mse')
model.fit(X_train, y_train, epochs=50)
```

---

## 🎯 μv2.0モデル（1着予測特化）

**最終更新**: 2025-11-24  
**モデルタイプ**: LightGBM Binary Classification  
**目的変数**: `is_win` (1着=1, その他=0)

### 概要

従来のμ/σ/νモデルに加え、**1着予測に特化した分類モデル**。完全にデータリークを排除し、的中率25.89%（ランダムの3.6倍）を達成。

### アーキテクチャ

```python
import lightgbm as lgb
import optuna

# ターゲット作成
train_df['is_win'] = (train_df['finish_position'] == 1).astype(int)

# LightGBM設定
params = {
    'objective': 'binary',
    'metric': 'auc',
    'boosting_type': 'gbdt',
    'num_leaves': 34,
    'feature_fraction': 0.66,
    'bagging_fraction': 0.93,
    'lambda_l1': 0.22,
    'lambda_l2': 0.0016,
    'feature_pre_filter': False  # リーク防止のため重要
}

model = lgb.train(
    params,
    lgb.Dataset(X_train, y_train),
    valid_sets=[lgb.Dataset(X_valid, y_valid)],
    num_boost_round=2000,
    callbacks=[lgb.early_stopping(50)]
)
```

### 特徴量（163個、完全リークフリー）

詳細は [06_特徴量エンジニアリング.md](./06_特徴量エンジニアリング.md) のμv2.0セクション参照。

**カテゴリ構成**:
1. 安定性・経験値（8特徴量）
2. 条件適性（15特徴量）
3. ペース適性（10特徴量）
4. 騎手・調教師相性（8特徴量）
5. 血統詳細（12特徴量）
6. レース間隔（5特徴量）

**除外したリーク源**（8種類）:
- `final_corner_to_finish`, `pace_index`, `passing_order_1-4`, `last_3f_time`, `time_except_last3f`, `position_change_*`, `win_probability`, `finish_time_seconds`

### データリーク防止

#### ローリングウィンドウ方式

各月のレースを予測する際、**その月の前月末までのデータのみ**を使用。

```python
# train_mu_v2_model.py
for current_month in training_period:
    # Target: 今月のレース
    target_mask = (df['race_date'] >= current_month) & (df['race_date'] < next_month)
    target_df = df[target_mask]
    
    # History: 今月より前の全データ
    history_mask = (df['race_date'] < current_month)
    history_df = df[history_mask]
    
    # 特徴量生成
    features = engine.generate_features(
        shutuba_df=target_df,
        results_history_df=history_df,
        ...
    )
```

#### `.shift(1)` による過去走保護

過去走集計特徴量は今回のレースを除外。

```python
# feature_engine.py
grouped = df.groupby('horse_id')
shifted = grouped['finish_position'].shift(1)  # 今回を除外

# 直近N走の統計
df['avg_finish_last3'] = shifted.rolling(3).mean()
```

### 性能

| 指標 | 検証（2023） | テスト（2024） | 評価 |
|------|------------|--------------|------|
| **AUC** | 0.7577 | 0.7612 | 過学習なし |
| **LogLoss** | - | 0.2308 | 良好 |
| **的中率（Top1）** | - | 25.89% | ランダムの3.6倍 |
| **的中率（Top3）** | - | 55.97% | - |

### 重要特徴量（Top 10）

| ランク | 特徴量 | 重要度 | カテゴリ |
|--------|--------|--------|----------|
| 1 | `surface_avg_finish` | 6,423 | 条件適性 |
| 2 | `trainer_horse_avg_finish` | 5,743 | 相性 |
| 3 | `jockey_win_rate` | 4,412 | 相性 |
| 4 | `age_zscore` | 1,842 | 基本 |
| 5 | `horse_weight_zscore` | 1,048 | 基本 |
| 6 | `jockey_trainer_avg_finish` | 934 | 相性 |
| 7 | `jockey_芝_win_rate` | 860 | 条件適性 |
| 8 | `trainer_horse_win_rate` | 854 | 相性 |
| 9 | `jockey_surface_win_rate` | 844 | 条件適性 |
| 10 | `distance_avg_finish` | 722 | 条件適性 |

**血統特徴量**: `sire_surf_avg_finish` (16位), `sire_dist_avg_finish` (20位) が上位にランクイン。

### 運用

```python
# 推論
import pickle

with open('keibaai/models/mu_v2/mu_v2_model.pkl', 'rb') as f:
    model = pickle.load(f)

pred_prob = model.predict(features)

# レースごとに順位付け
df['rank_pred'] = df.groupby('race_id')['pred_prob'].rank(ascending=False)
top1 = df[df['rank_pred'] == 1]  # 予測1位
```

### 評価結果

生成済み: `keibaai/data/evaluation/evaluation_results_mu_v2_*.csv`

詳細レポート: [`mu_v2_final_report.md`](../../.gemini/antigravity/brain/360a6469-133a-4108-89c3-c01cad3d127a/mu_v2_final_report.md)

---

**次のドキュメント**: [08_シミュレーションと最適化.md](./08_シミュレーションと最適化.md)
