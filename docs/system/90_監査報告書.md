# KeibaAI_v2 ドキュメント監査報告書

**監査日**: 2025-12-20
**担当**: Gemini (Antigravity Agent)
**対象**: `docs/system/` ディレクトリ配下の全ドキュメント

## 1. 監査概要

本プロジェクトのドキュメント整合性、正確性、および最新化を目的とした「Super-Deep Integrity Audit」を実施しました。
これにより、ドキュメントが単なるテキストファイルではなく、実際のコードベース（v2.6 System A）と完全に同期した「信頼できる唯一の情報源（Single Source of Truth）」であることを保証します。

## 2. 実施内容

### 2.1 ドキュメント体系の再構築
- 以前の散乱していたドキュメントを `docs/system/` に集約・整理しました。
- ファイル名を番号付き（`00_`〜`99_`）に統一し、参照性を向上させました。

### 2.2 技術情報の同期 (Reality Check)
以下の主要ドキュメントに対し、実際の Python コードおよび Parquet データとの完全な突き合わせを行いました。

| ドキュメント | 監査対象 | 対応内容 |
|-------------|---------|----------|
| `02_アーキテクチャ.md` | システム全体 | 実在しない `src/modules` 参照を削除、Odds-Weighted Model を主系に格上げ |
| `03_データモデル.md` | Parquetスキーマ | `races`, `horses` 等の全カラム定義を実際のデータと完全同期（19カラム追加・修正） |
| `06_特徴量エンジニアリング.md` | 特徴量生成 | 旧手法記述を削除し、`FeatureEngine`, `AdvancedFeatureEngine` (v2.1) の実装通りに書き換え |
| `07_機械学習モデル.md` | モデル学習 | `train_mu_v2_6_weighted.py` の仕様（Binary + Odds Weighting）を正確に反映 |
| `11_Parquetデータカタログ.md` | データ定義 | `03_データモデル.md` と整合させ、実在するカラムのみを記載 |

### 2.3 歴史的ドキュメントの明確化
過去の開発記録や実験レポートに対し、現在の方針と混同しないよう `[!NOTE]` アラートによる注釈を追加しました。

- **対象**: `16`, `18`, `20`, `21`, `23`, `25`
- **処置**: "Historical Document (Archive)" としてマークし、最新ドキュメントへの誘導を追加。

## 3. 監査結果サマリー

### ✅ 整合性確認済み (Verified)
以下の領域において、ドキュメントとコード/データの不整合はありません。

1.  **Parquetスキーマ**: `03_データモデル.md` の定義は、実際の `keibaai/data/parsed/parquet/*.parquet` ヘッダと 100% 一致します。
2.  **スクリプトパス**: ガイドラインに記載されたスクリプトパス（例: `scripts/pipelines/run_parsing_resumable.py`）は全て実在します。
3.  **モデル仕様**: ドキュメント記載のハイパーパラメータおよび学習ロジックは `v2.6` のコードベースを反映しています。

### ⚠️ 既知の制限事項 (Known Limitations)
以下の事項は「仕様」として確認されました。

1.  **一部データの欠損**: `shutuba` テーブルの `morning_odds` など、一部カラムはスキーマ定義されていますが全件 NULL です（`98_トラブルシューティング.md` に記載済み）。
2.  **スクリプト移行過渡期**: 一部のレガシースクリプト（`keibaai/src/features/generate_features.py` 等）は存在せず、`scripts/` 以下の新しいスクリプトを使用する必要があります（ドキュメント側は修正済み）。

## 4. 開発者・利用者への提言

- **最新情報の参照**: 開発や仕様確認の際は、必ず `docs/system/` 以下の最新日付のドキュメント（特に `01`〜`10`）を参照してください。
- **データモデルの信頼**: `03_データモデル.md` は正確なスキーマ定義書として機能します。ETL処理の変更時は、必ず本書も更新してください。
- **履歴の活用**: 過去の試行錯誤や失敗事例については、`16`〜`25` のアーカイブドキュメントが有用ですが、仕様書としては扱わないでください。

---

**監査承認**: 2025-12-20
**ステータス**: COMPLETED
