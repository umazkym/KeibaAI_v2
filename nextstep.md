ROI（回収率）最大化モデル「μモデル v2.6」に向けた、具体的かつ実装レベルの特徴量定義レポートを作成しました。
現在のデータ（parquet_contents_report.md）を突き合わせ、**「今すぐ計算できるもの」**と**「計算するために外部データ（マスタ）が必要なもの」**を明確に区別しています。

---

# ROI最大化・特化型特徴量設計レポート

## 1. タイムとペースの「補正」指標 (A. Time & Pace)
生のタイムではなく、「ペース配分」に注目して、隠れた能力を炙り出す指標です。

### ① `avg_pci_last5` （過去5走PCI平均）
**【目的】**
スローペースでしか勝てない馬や、ハイペースでバテたが実は強かった馬を見分けるため。

* **わかりやすい解説:**
    PCI（Pace Change Index）とは、「前半のペース」と「後半の瞬発力（上がり3F）」のバランス値です。
    * 50未満＝ハイペース（消耗戦）
    * 50以上＝スローペース（瞬発力勝負）
    これの過去平均を見ることで、「この馬が得意な展開」を数値化します。

* **計算式:**
    1.  **RPCI（レースペース指数）**:
        $$\text{前半3F換算} = (\text{走破タイム} - \text{上がり3F}) \div (\text{距離} - 600m) \times 600$$
        $$\text{PCI} = \frac{59.5 - \text{上がり3F}}{59.5 - \text{前半3F換算}} \times 100 + 50$$
    2.  過去5走分の平均をとる。

* **必要なデータ:**
    * `races.finish_time_seconds` (走破タイム秒)
    * `races.last_3f_time` (上がり3F)
    * `races.distance_m` (距離)
    * **判定:** ✅ **計算可能**

### ② `nishida_trend_score` （西田式指数の近走トレンド）
**【目的】**
「時計のかかる馬場」や「高速馬場」といった外部要因を取り除いた、純粋なスピード能力の推移を見るため。

* **わかりやすい解説:**
    「東京芝1600mの1分33秒」と「函館芝1200mの1分9秒」、どちらが速いかを比較するために、コースごとの「基準タイム」との差分をとったもの。

* **計算式:**
    $$\text{スピード指数} = (\text{基準タイム} - \text{走破タイム}) \times \text{距離指数} + \text{馬場差補正} + (\text{斤量} - 55) \times 2 + 80$$
    * ここから過去3走の推移（上昇トレンドか下降トレンドか）をスコア化。

* **必要なデータ:**
    * `races.finish_time_seconds`
    * `races.basis_weight` (斤量)
    * `races.track_condition` (馬場状態)
    * ⚠️ **不足データ:** **「コース別基準タイムマスタ」**
        * 各競馬場・距離・クラスごとの「平均勝ちタイム」の定数表が必要です（例：東京芝2400m古馬1勝クラス＝2:25.0 など）。これがないと計算できません。

---

## 2. トラックバイアスと「不利」の数値化 (B. Bias & Disadvantage)
「負けた理由」が馬の能力不足ではないケースを拾います。

### ③ `corner_loss_proxy` （コーナー距離ロス・プロキシ）
**【目的】**
「外枠から終始外々を回らされた」距離ロスによる敗戦を救済するため。

* **わかりやすい解説:**
    競馬場のコーナーで内ラチ沿いを走るのと、大外を走るのとでは、走る距離が10m以上変わることがあります。
    正確な「何頭分外を回ったか」というデータはお持ちのParquetにはありませんが、**「枠順」と「通過順」で擬似的に計算**します。

* **計算ロジック (代用案):**
    * もし `bracket_number`（枠番）が 7 or 8（外枠） かつ、
    * `passing_order_3` または `passing_order_4`（3,4コーナー通過順）が、出走頭数の半分より上位（先行している）場合
    * → 「外枠から無理に先行して外を回した可能性大」として、距離ロス係数を付与する。

* **必要なデータ:**
    * `races.bracket_number` (枠番)
    * `races.passing_order_3`, `passing_order_4` (通過順)
    * `shutuba.bracket_number` (今回の枠)
    * **判定:** ✅ **計算可能（簡易版として）**

---

## 3. 血統と適性の「特異点」 (C. Pedigree)
平均的な強さではなく、「特定の条件」での強さを探します。

### ④ `sire_wet_track_boost` （種牡馬・道悪ブースト率）
**【目的】**
「雨が降ると突然激走する血統」を見つけるため。

* **わかりやすい解説:**
    父馬（Sire）ごとに、「良馬場での勝率」と「重・不良馬場での勝率」を比較します。
    「良馬場勝率 5%」だが「不良馬場勝率 15%」のような種牡馬の産駒が、雨の日に出走してきたら買いです。

* **計算ロジック:**
    1.  `pedigrees` から generation=1 (父) を取得し、`horses` と結合。
    2.  `races` データを使い、父馬ごとに集計。
    3.  $A = \text{良馬場勝率}$
    4.  $B = \text{重・不良馬場勝率}$
    5.  $\text{Boostスコア} = B - A$ （または $B / A$）
    6.  今回出走する馬の父のスコアを特徴量として結合。

* **必要なデータ:**
    * `pedigrees` (父ID特定のため)
    * `races.track_condition` (良・稍重・重・不良)
    * `races.finish_position` (着順)
    * **判定:** ✅ **計算可能**

### ⑤ `pedigree_course_compatibility` （血統×コース相性値）
**【目的】**
「中山芝2500m」のようなトリッキーなコースで、特異的に強い血統を見つけるため。

* **計算ロジック:**
    1.  父馬（Sire）× コース条件（場所+距離+芝ダート）ごとに複勝率を集計。
    2.  例：「ハーツクライ産駒 × 東京芝2400m」の複勝率 = 40%
    3.  全種牡馬の平均複勝率との乖離をスコア化。

* **必要なデータ:**
    * `races.venue` (開催場所)
    * `races.distance_m` (距離)
    * `races.track_surface` (芝/ダート)
    * `pedigrees`
    * **判定:** ✅ **計算可能**

---

## 4. レースレベルとメンバー構成 (D. Field Level)
「誰と戦ってきたか」の文脈を評価します。

### ⑥ `prev_race_level_index` （前走レースレベル指数）
**【目的】**
「弱い相手に勝った馬」より「強い相手に善戦した馬」を高く評価するため。

* **わかりやすい解説:**
    前走走ったレースに出走していた**ライバルたちの「その後」**を追跡します。
    ライバルたちが次走以降で勝ち上がっていれば、そのレースは「ハイレベル戦」だったと言えます。

* **計算ロジック:**
    1.  対象馬の前走の `race_id` を特定。
    2.  そのレースに出走していた全馬（自分以外）を取得。
    3.  そのライバルたちの「そのレース以降の日付」の成績を参照。
    4.  $\text{ライバル勝率} = \frac{\text{ライバルたちの次走以降の勝利数合計}}{\text{ライバルたちの次走以降の出走数合計}}$
    5.  この「ライバル勝率」が高ければ、前走の着順が悪くても評価を上げる。

* **必要なデータ:**
    * `races.race_id`
    * `races.horse_id`
    * `races.race_date`
    * **判定:** ✅ **計算可能**

---

## 5. オッズと市場心理の「裏」 (E. Market Inefficiency)
市場の歪み（過小評価）を直接狙い撃ちます。

### ⑦ `under_valued_score_avg` （過小評価スコア平均）
**【目的】**
常に「人気の割に好走する」馬を見つけるため（馬券巧者が狙い続けている馬）。

* **わかりやすい解説:**
    「10番人気で3着」「8番人気で4着」のように、期待値以上の走りをする癖がある馬を数値化します。

* **計算ロジック:**
    1.  各レースで $\text{乖離} = \text{人気ランク} - \text{着順}$ を計算。
        * 例：10番人気 - 3着 = +7ポイント（優秀）
        * 例：1番人気 - 10着 = -9ポイント（期待外れ）
    2.  これを過去5走分平均する。
    3.  値がプラスであれば「常に人気以上に走る過小評価馬」である。

* **必要なデータ:**
    * `races.popularity` (人気)
    * `races.finish_position` (着順)
    * **判定:** ✅ **計算可能**

---

# まとめ：必要なアクションリスト

今回の特徴量セットを作成するために、以下の準備が必要です。

1.  **外部マスタデータの収集（必須）**
    * **基準タイムマスタ**: 西田式スピード指数等の計算に不可欠です。自分で `races` データを全件集計し、「過去3年の同コース・同クラスの平均タイム」を計算して基準タイムとして代用する必要があります。

2.  **データ加工の実装**
    * 上記の論理に従い、特徴量生成コードを作成する必要があります。

3.  **「コーナー通過詳細」について**
    * 現在のデータには「何頭分外を回ったか」という詳細情報はありません。一旦は `bracket_number` (枠) と `passing_order` (通過順) を使った「プロキシ（代替）指標」で実装を進めることを推奨します。